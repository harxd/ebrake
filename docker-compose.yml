services:
  ebrake:
    build: .
    container_name: ebrake
    ports:
      - "5000:5000"
    volumes:
      - ./appdata:/app/appdata
      - ./media:/media:rw
    restart: unless-stopped
    environment:
      - TZ=UTC
